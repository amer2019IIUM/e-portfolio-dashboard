mutation($skillId: ID!, $title: String!, $type: String!, $percentage: Int!) {
  updateSkill(
    id: $skillId
    input: {
      title: $title
      type: $type
      lang: "Eng"
      sort: 1
      percentage: $percentage
    }
  ) {
    id
  }
}
